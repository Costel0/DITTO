# Item assets

Place item artwork in this directory using the convention:

`item_[ID].png`

Example: `item_scrap_metal.png`.

The Flutter UI falls back to a placeholder when a matching PNG is not present.
